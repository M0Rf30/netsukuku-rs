[package]
name = "ntk-common"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Shared base types for netsukuku-rs: topology, hierarchical addressing, fingerprints, path cost."
readme = "README.md"
homepage.workspace = true
keywords = ["netsukuku", "mesh", "hierarchical", "addressing", "topology"]
categories = ["data-structures", "network-programming", "algorithms"]

[dependencies]
thiserror = { workspace = true }
serde = { workspace = true, optional = true, features = ["derive"] }

[dev-dependencies]
proptest = { workspace = true }

[features]
default = ["serde"]
serde = ["dep:serde"]

[lints]
workspace = true
